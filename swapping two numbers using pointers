#include <stdio.h>
void swap_numbers(int *a, int *b)
{
  int temp;
  temp = *a;
  *a = *b;
  *b = temp;
}
void main() 
{
  int a,b;
  printf("Enter two numbers: ");
  scanf("%d %d", &a, &b);
  swap_numbers(&a, &b);
  printf("The swapped numbers are: %d %d", a, b);
}

